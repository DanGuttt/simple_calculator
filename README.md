Ce code permet de réaliser une addition entre deux nombres positifs et inférieurs à 1000. Voici comment ce code fonctionne :
récupère les deux valeurs, les fait passer dans une multitude de fonctions récursives. Le résultat se fait passer dans une requête à api hébergée par nos soins. La réponse de la requête est interprété dans un navigateur et se fait récupérer dans le code source en parsing.
le resultat se fait soustraire 5 puis est passé dans une ia qui additionne ce résultat avec 5. Trivial.
